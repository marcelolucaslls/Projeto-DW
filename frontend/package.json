{
  "name": "monitor-ad",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server --watch db.json --port 5000" 
  },
  "keywords": ["monitoramento", "rede", "json-server"],
  "author": "",
  "license": "ISC",
  "description": "Sistema de monitoramento de rede utilizando JSON Server",
  "devDependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
